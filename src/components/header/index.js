import { H2, HeaderWrapper, Input } from "./styles";

function Header() {
     return (
          <HeaderWrapper>
               <H2> Contador </H2>

               <H2>Numero</H2>

          </HeaderWrapper>
     );
}

export default Header;